<?php if ( $list->get('data.conversation_message_id') ) : ?>
    <div class="inner">
        <?= $list->get('data.lastpost.user') ?>
        <br>
        <span class="date"><?= $list->get('data.lastpost.date') ?></span>
    </div>
    <?= $list->get('data.lastpost.user_image') ?>
<?php else : ?>
    <span class="fw-600"><?= $this->language->get('L_USER.L_CONVERSATION.L_MESSAGE.L_NO') ?></span>
<?php endif; ?>