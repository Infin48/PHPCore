
<?php if ($body = $this->build->user->notification($row['data'])) : ?>
    <?= $this->build->user->image($row['data']) ?>
    <div class="inner">
        <?= $body ?>
        <br>
        <?= $this->build->date->long($row['data']['user_notification_created'], true) ?>
    </div>
<?php else : ?>
    <div>
        <?= $L_NAVBAR['L_NOTIFICATION_EMPTY'] ?>
    </div>
<?php endif; ?>